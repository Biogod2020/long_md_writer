# LongWriter DSH adapter

This package is an optional runtime adapter. It maps DSH Goals, Sessions and
fresh child agents onto the harness-agnostic `@longwriter/core` contracts. No
publication state or validation rule is owned here.

Install the repository workspace, then add this package to a dedicated DSH
profile. The legacy `dsh-native/` directory remains as a compatibility entrypoint.
