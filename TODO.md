# Remaining production gates

- [ ] Run the credentialed Agents SDK -> Codex smoke workflow in GitHub Actions.
- [ ] Run a complete candidate against `v18_comprehensive_run` and archive the
      generated `.magnum/baseline-comparison.json` in the PR evidence.
- [ ] Benchmark draft concurrency and API cost on representative long documents.
- [ ] Add citation-resolution plugins for source types that provide stable identifiers.
