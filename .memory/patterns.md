# PATTERNS
- **git/workspace-hygiene/user-preference**: Explicitly whitelist 'inputs/' and 'assets/' in .gitignore using '!inputs/**' to protect project materials from broad root-level ignore patterns (like '/*.png'). (from mcp-1773798347995)
