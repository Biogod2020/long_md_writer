# RCA & Quality Restoration Plan

## 🎯 Goal
Identify and fix the root causes of the quality regression in `final_v3` (visual intent dilution and fulfillment commit failure).

## 🔍 Investigation Steps (Phase 1)
- [x] **Comparative Audit (Intent)**: Compare `visual_directives` generated in `final_e2e_v2` vs `final_v3`. 
- [x] **Comparative Audit (Prompt)**: Review the system instructions for `WriterAgent` during both runs. Did the prompt change or lose context?
- [x] **Commit Logic Audit**: Trace the `WorkingCopyManager.commit()` execution in `final_v3` logs. Why did `.working` files persist?
- [x] **Refiner Check**: Analyze the `project_brief` generated by the Refiner. Did `final_v3` lose the "SOTA Aesthetic" requirement in the brief? (Confirmed: Requirement remains in system prompt).

## 🛠️ Implementation (Phase 2)
- [x] **Harden Writer Prompt**: Inject mandatory high-fidelity visual schema into the Writer's core instructions.
- [x] **Refine Patcher Logic**: Fix the atomic swap in `WorkingCopyManager` to handle concurrent I/O or existing file locks.
- [x] **Quality Gate**: (Dismissed by user - manual oversight preferred).

## 🚀 Validation (Phase 3)
- [x] Run a fresh E2E task `quality_verified_v1` and compare with `final_e2e_v2`. (Completed by user).
