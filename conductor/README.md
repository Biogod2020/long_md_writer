# Historical Conductor records

The previous repository accumulated implementation tracks for LangGraph, Gemini
proxy adaptations, custom patchers, and recovery harnesses. They are intentionally
not part of the active migrated tree because they describe a retired runtime and
create conflicting instructions for coding agents.

The complete history remains available in Git. New architecture decisions belong in
`docs/` and executable acceptance criteria belong in `tests/`.
