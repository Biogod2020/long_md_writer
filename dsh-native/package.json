{
  "name": "long-md-writer-dsh-native",
  "version": "0.2.0",
  "description": "Compatibility entrypoint for the optional LongWriter DSH adapter",
  "type": "module",
  "main": "index.js",
  "files": [
    "index.js",
    "cordis.patch.yml",
    "README.md",
    "DSH_COMPATIBILITY.md",
    "examples/*.json"
  ],
  "license": "MIT",
  "engines": { "node": ">=22" },
  "dsh": {
    "bundle": { "patch": "./cordis.patch.yml" }
  },
  "dependencies": {
    "@deepseek-ai/dsh-llm": "0.1.0-rc.6",
    "@deepseek-ai/dsh-tools": "0.1.0-rc.6",
    "@longwriter/core": "workspace:*"
  },
  "scripts": {
    "check": "node --check index.js && node -e \"import('./index.js').then(m => { if (typeof m.apply !== 'function') process.exit(1) })\"",
    "test": "node --test ../adapters/dsh/test/*.test.js"
  }
}
